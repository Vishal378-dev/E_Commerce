export type variantAttribute = {
  name: string;
  values: string[];
};

export type variant = {
  name: string;
  description: string;
  price: number;
  stock: number;
  category: string;
  subCategory: string;
  weight: number;
  length: number;
  height: number;
  width: number;
  images: any;
  video: string;
  specification: any;
  variantName: string;
  variantId: string;
  productId: string;
  wasPrice: number;
};
